import { useEffect,useRef } from 'react';
export function DetailPanel({title,content,onClose}:{title:string;content:string;onClose:()=>void}){const ref=useRef<HTMLDivElement>(null);useEffect(()=>{ref.current?.focus();const key=(e:KeyboardEvent)=>{if(e.key==='Escape')onClose()};document.addEventListener('keydown',key);return()=>document.removeEventListener('keydown',key)},[onClose]);return <div className="modal-backdrop" onMouseDown={e=>{if(e.target===e.currentTarget)onClose()}}><div className="modal" role="dialog" aria-modal="true" aria-labelledby="detail-title" tabIndex={-1} ref={ref}><button className="modal__close" onClick={onClose} aria-label="Fechar detalhes">×</button><h2 id="detail-title">{title}</h2><p>{content||'conteúdo indisponível'}</p></div></div>}
